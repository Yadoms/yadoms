#place here all libraries

#SQLite
IF(USE_SQLITE_DATABASE)
	add_subdirectory(SQLite)
	set(SQLITE_INCLUDE_DIRECTORY ${SQLITE_INCLUDE_DIRECTORY} PARENT_SCOPE)
ENDIF()

#Libusbpp
if(NOT WIN32)
   add_subdirectory(Libusbpp)
   set(LIBUSBPP_FOUND ${LIBUSBPP_FOUND} PARENT_SCOPE)
   set(LIBUSBPP_INCLUDE_DIRS ${LIBUSBPP_INCLUDE_DIRS} PARENT_SCOPE)
   set(LIBUSBPP_LIBRARIES ${LIBUSBPP_LIBRARIES} PARENT_SCOPE)
endif(NOT WIN32)