(function () {
   'use strict';

   var getSupportedLanguages = function() {
      return {
         "fr": "Français",
         "en": "English"
      };
   }
  
   window.getSupportedLanguages = getSupportedLanguages;
})();
