# This file contains user specific configuration
# It may contains some directory definitions

#find Gammu method selection for linux
#set USE_PKGCONFIG will use PkgConfig tool to find installed Gammu in the system
#if you want to use your own version of Gammu, set USE_PKGCONFIG to OFF and
#define the GAMMU_ROOT to indicate to cmake where is your Gammu version.
set(USE_PKGCONFIG ON)

#define Gammu ROOT
##### use only absolute path , and do not use anti slash : only '/' ######
# For Windows
set(GAMMU_ROOT "/path/to/Gammu/gammu-1.33.0")

# For Raspberry cross-compilation
#set(GAMMU_ROOT "/path/to/Gammu/gammu-1.33.0/raspberrypi")

