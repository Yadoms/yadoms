info:
  name: Mise à jour règle
  type: http
  seq: 6

http:
  method: PUT
  url: http://{{yadoms_ip}}:{{yadoms_port}}/rest/automation/rule/1
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "enabled": "true"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
