info:
  name: Device keywords for a capacity
  type: http
  seq: 5

http:
  method: GET
  url: http://{{yadoms_ip}}:{{yadoms_port}}/rest/device/1/get/temperature
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
