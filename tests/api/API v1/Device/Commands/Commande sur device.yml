info:
  name: Commande sur device
  type: http
  seq: 2

http:
  method: POST
  url: http://{{yadoms_ip}}:{{yadoms_port}}/rest/device/3/command
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "mySpecificDeviceCommand": "hello !"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
