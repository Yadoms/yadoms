info:
  name: Device matching capacity
  type: http
  seq: 2

http:
  method: GET
  url: http://{{yadoms_ip}}:{{yadoms_port}}/rest/device/matchcapacity/get/temperature
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
