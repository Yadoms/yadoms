info:
  name: Liste des devices
  type: http
  seq: 6

http:
  method: GET
  url: http://{{yadoms_ip}}:{{yadoms_port}}/rest/device
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
