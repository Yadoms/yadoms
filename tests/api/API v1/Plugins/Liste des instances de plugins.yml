info:
  name: Liste des instances de plugins
  type: http
  seq: 3

http:
  method: GET
  url: http://{{yadoms_ip}}:{{yadoms_port}}/rest/plugin/instance
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
