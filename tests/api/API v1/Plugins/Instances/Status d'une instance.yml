info:
  name: Status d'une instance
  type: http
  seq: 3

http:
  method: GET
  url: http://{{yadoms_ip}}:{{yadoms_port}}/rest/plugin/4/status
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
