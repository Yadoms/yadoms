info:
  name: delete plugin instances
  type: http
  seq: 5

http:
  method: DELETE
  url: http://{{yadoms_ip}}:{{yadoms_port_v2}}/rest/v2/plugins-instances/:idPluginInstance
  params:
    - name: idPluginInstance
      value: "20"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
